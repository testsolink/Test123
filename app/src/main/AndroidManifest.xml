<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".basic.BasicActivity"
            android:label="@string/basic">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".index.MainActivity" />
        </activity>
        <activity
            android:name=".advance.AdvanceActivity"
            android:label="@string/advance">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".index.MainActivity" />
        </activity>
        <activity android:name=".index.MainActivity" />
        <activity android:name=".grid_view.GridViewActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".basic.BasicActivity" />
        </activity>
        <activity android:name=".about_view.AboutViewActivity" />
        <activity android:name=".list_view.ListViewActivity" />
        <activity android:name=".tab_view.TabVIewActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".basic.BasicActivity" />
        </activity>
    </application>

</manifest>